"use client";import{useState}from"react";import Link from"next/link";import{articles}from"@/content/data";
export function ContentBrowser(){const[q,setQ]=useState('');const[cat,setCat]=useState('');const list=articles.filter(a=>(!cat||a.category===cat)&&a.title.toLowerCase().includes(q.toLowerCase()));return <><div className="mt-8 grid gap-4 md:grid-cols-2"><label>Buscar por título<input className="mt-2 w-full rounded border p-3" value={q} onChange={e=>setQ(e.target.value)}/></label><label>Filtrar por área<select className="mt-2 w-full rounded border p-3" value={cat} onChange={e=>setCat(e.target.value)}><option value="">Todas</option>{[...new Set(articles.map(a=>a.category))].map(c=><option key={c}>{c}</option>)}</select></label></div><div className="mt-8 grid gap-5 md:grid-cols-2">{list.map(a=><article className="card p-6" key={a.slug}><p className="eyebrow">{a.category}</p><h2 className="mt-3 text-2xl text-[#0a2038]">{a.title}</h2><p className="my-4">{a.summary}</p><Link className="font-bold" href={`/conteudos/${a.slug}`}>Ler artigo →</Link></article>)}{!list.length&&<p>Nenhum conteúdo encontrado.</p>}</div><nav className="mt-8" aria-label="Paginação"><span aria-current="page" className="rounded bg-[#0a2038] px-4 py-2 text-white">1</span></nav></>}
