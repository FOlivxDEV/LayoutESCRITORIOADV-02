import Link from "next/link";export function Breadcrumbs({items}:{items:{label:string,href?:string}[]}){return <nav aria-label="Navegação estrutural" className="mb-8 text-sm"><ol className="flex flex-wrap gap-2">{items.map((i,n)=><li key={i.label}>{n>0&&<span className="mr-2">/</span>}{i.href?<Link className="underline" href={i.href}>{i.label}</Link>:<span aria-current="page">{i.label}</span>}</li>)}</ol></nav>}
