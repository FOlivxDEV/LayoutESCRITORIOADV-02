"use client";
import { useState } from "react";
export function CookieBanner(){const[show,setShow]=useState(()=>typeof window!=="undefined"&&!localStorage.getItem("cookie-consent"));const save=(v:string)=>{localStorage.setItem("cookie-consent",v);setShow(false)};if(!show)return null;return <section className="fixed inset-x-4 bottom-4 z-50 mx-auto max-w-3xl rounded-lg border bg-white p-5 shadow-2xl" role="dialog" aria-label="Preferências de cookies"><p className="font-bold text-[#0a2038]">Sua privacidade importa</p><p className="my-2 text-sm">Usamos cookies necessários para o funcionamento. Analytics e marketing ficam desativados até sua escolha.</p><div className="flex flex-wrap gap-2"><button className="btn btn-primary" onClick={()=>save("all")}>Aceitar todos</button><button className="btn btn-outline" onClick={()=>save("necessary")}>Aceitar necessários</button><button className="btn btn-outline" onClick={()=>save("rejected")}>Rejeitar não essenciais</button><a className="btn btn-outline" href="/politica-de-cookies">Personalizar</a></div></section>}
