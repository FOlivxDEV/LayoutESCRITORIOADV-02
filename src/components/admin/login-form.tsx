"use client";import{useState}from"react";import{createBrowserClient}from"@supabase/ssr";import{useRouter}from"next/navigation";
export function LoginForm(){const[msg,setMsg]=useState('');const router=useRouter();async function login(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setMsg('');const form=new FormData(e.currentTarget);const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key){setMsg('Autenticação ainda não configurada.');return}const client=createBrowserClient(url,key);const{error}=await client.auth.signInWithPassword({email:String(form.get('email')),password:String(form.get('password'))});if(error){setMsg('Não foi possível entrar com as credenciais informadas.');return}router.push('/admin');router.refresh()}return <form className="card mt-8 p-7" onSubmit={login}><label className="block">E-mail<input className="mt-2 w-full rounded border p-3" type="email" name="email" autoComplete="username" required/></label><label className="mt-5 block">Senha<input className="mt-2 w-full rounded border p-3" type="password" name="password" autoComplete="current-password" required/></label><button className="btn btn-primary mt-6 w-full">Entrar</button><p className="mt-4 text-sm" aria-live="polite">{msg}</p></form>}
