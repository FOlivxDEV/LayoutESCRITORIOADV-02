import type{Metadata}from"next";import{notFound}from"next/navigation";import Link from"next/link";import{areas,faqs,disclaimer}from"@/content/data";import{Breadcrumbs}from"@/components/ui/breadcrumbs";import{JsonLd}from"@/components/ui/json-ld";
export function generateStaticParams(){return areas.map(a=>({slug:a.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const a=areas.find(x=>x.slug===slug);return a?{title:a.name,description:a.description,alternates:{canonical:`/areas/${a.slug}`}}:{}}
export default async function AreaPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const a=areas.find(x=>x.slug===slug);if(!a)notFound();const q=faqs.slice(0,4);return <div className="section container pt-32"><JsonLd data={{'@context':'https://schema.org','@type':'LegalService',name:a.name,description:a.description}}/><Breadcrumbs items={[{label:'Início',href:'/'},{label:'Áreas',href:'/#areas'},{label:a.name}]}/><p className="eyebrow">Área de atuação</p><h1 className="title mt-3">{a.name}</h1><p className="mt-6 max-w-3xl text-lg leading-8">{a.description}</p><section className="mt-12"><h2 className="serif text-3xl text-[#0a2038]">Temas relacionados</h2><ul className="mt-5 grid gap-3 md:grid-cols-2">{a.topics.map(t=><li className="card p-5" key={t}>✓ {t}</li>)}</ul></section><section className="mt-12"><h2 className="serif text-3xl text-[#0a2038]">Perguntas frequentes</h2>{q.map(f=><details className="mt-3 border-b p-4" key={f[1]}><summary className="cursor-pointer font-bold">{f[1]}</summary><p className="mt-3">{f[2]}</p></details>)}</section><p className="mt-12 rounded bg-[#f5f0e7] p-5 text-sm">{disclaimer}</p><Link className="btn btn-primary mt-7" href="/#contato">Fale com nossa equipe</Link></div>}
