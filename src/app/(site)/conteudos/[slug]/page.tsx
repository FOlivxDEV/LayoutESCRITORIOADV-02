import type{Metadata}from"next";import{notFound}from"next/navigation";import{articles,disclaimer}from"@/content/data";import{Breadcrumbs}from"@/components/ui/breadcrumbs";import{JsonLd}from"@/components/ui/json-ld";
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const a=articles.find(x=>x.slug===slug);return a?{title:a.title,description:a.summary,openGraph:{type:'article',title:a.title,description:a.summary}}:{}}
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const a=articles.find(x=>x.slug===slug);if(!a)notFound();const date='2026-07-22';return <article className="section container max-w-4xl pt-32"><JsonLd data={{'@context':'https://schema.org','@type':'Article',headline:a.title,description:a.summary,datePublished:date,dateModified:date,author:{'@type':'Organization',name:'Ferraz, Oliveira & Martins Advocacia'}}}/><Breadcrumbs items={[{label:'Início',href:'/'},{label:'Conteúdos',href:'/conteudos'},{label:a.title}]}/><p className="eyebrow">{a.category}</p><h1 className="title mt-3">{a.title}</h1><p className="mt-5 text-lg">{a.summary}</p><p className="mt-4 text-sm text-slate-500">22 de julho de 2026 · Atualizado em 22 de julho de 2026 · Equipe editorial · 4 min de leitura</p><div className="mt-10 space-y-6 text-lg leading-8"><p>Este artigo apresenta uma visão geral e educativa do tema. Conceitos e procedimentos podem variar conforme os fatos, documentos e normas aplicáveis.</p><h2 className="serif text-3xl text-[#0a2038]">Por que a análise individual importa?</h2><p>Cada situação possui circunstâncias próprias. Antes de tomar decisões, reúna informações básicas e procure orientação profissional habilitada.</p><h2 className="serif text-3xl text-[#0a2038]">Próximos passos</h2><p>Evite compartilhar dados sensíveis em canais abertos. No primeiro contato, descreva apenas a área e a natureza geral da sua necessidade.</p></div><p className="mt-10 rounded bg-[#f5f0e7] p-5 text-sm">{disclaimer}</p></article>}
