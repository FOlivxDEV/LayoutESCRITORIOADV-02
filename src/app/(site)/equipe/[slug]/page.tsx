import type{Metadata}from"next";import Image from"next/image";import{notFound}from"next/navigation";import Link from"next/link";import{team,disclaimer}from"@/content/data";import{Breadcrumbs}from"@/components/ui/breadcrumbs";
export function generateStaticParams(){return team.map(p=>({slug:p.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const p=team.find(x=>x.slug===slug);return p?{title:p.name,description:`Perfil demonstrativo — ${p.role}, ${p.area}.`}:{}}
export default async function TeamPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=team.find(x=>x.slug===slug);if(!p)notFound();return <div className="section container pt-32"><Breadcrumbs items={[{label:'Início',href:'/'},{label:'Equipe',href:'/#equipe'},{label:p.name}]}/><div className="grid gap-10 md:grid-cols-[360px_1fr]"><div className="relative aspect-[4/5] overflow-hidden rounded"><Image src={p.image} alt={`Retrato fictício de ${p.name}`} fill className="object-cover"/></div><div><p className="eyebrow">Perfil demonstrativo</p><h1 className="title mt-3">{p.name}</h1><p className="mt-4 text-xl text-[#8a6e38]">{p.role}</p><p className="mt-2">{p.area}</p><p className="mt-2 text-sm">{p.oab}</p><p className="mt-8 leading-8">Perfil institucional fictício criado exclusivamente para demonstração do modelo. Formação, experiência, credenciais e registros devem ser substituídos e verificados antes da publicação.</p><p className="mt-8 rounded bg-[#f5f0e7] p-5 text-sm">{disclaimer}</p><Link className="btn btn-primary mt-7" href="/#contato">Entrar em contato</Link></div></div></div>}
