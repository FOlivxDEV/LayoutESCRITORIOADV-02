import type{MetadataRoute}from"next";import{areas,articles,team}from"@/content/data";import{siteConfig}from"@/config/site";export default function sitemap():MetadataRoute.Sitemap{const paths=['','/escritorio','/conteudos','/duvidas','/politica-de-privacidade','/politica-de-cookies','/termos-de-uso','/aviso-legal',...areas.map(a=>`/areas/${a.slug}`),...team.map(p=>`/equipe/${p.slug}`),...articles.map(a=>`/conteudos/${a.slug}`)];return paths.map(path=>({url:`${siteConfig.url}${path}`,lastModified:new Date(),changeFrequency:path.startsWith('/conteudos/')?'monthly':'yearly',priority:path===''?1:.7}))}
