import { createHash,randomBytes } from "node:crypto";import { NextRequest,NextResponse } from "next/server";import { contactSchema,normalizeEmail,normalizePhone,sanitizeText } from "@/lib/validation/contact";import { hasExcessiveUrls } from "@/lib/security/compliance";import { rateLimit } from "@/lib/rate-limit";import { sendContactEmails } from "@/lib/email/contact-email";
const generic='Não foi possível concluir a solicitação agora. Tente novamente em alguns instantes ou utilize outro canal de contato.';
export async function POST(req:NextRequest){try{const len=Number(req.headers.get('content-length')??0);if(len>12000)return NextResponse.json({message:generic},{status:413});const parsed=contactSchema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({message:'Verifique os campos informados.'},{status:400});const d=parsed.data;if(d.website||Date.now()-d.startedAt<2500||hasExcessiveUrls(d.message))return NextResponse.json({message:'Solicitação recebida.'});const email=normalizeEmail(d.email);const phone=normalizePhone(d.phone);const ip=req.headers.get('x-forwarded-for')?.split(',')[0]?.trim()??'unknown';const secret=process.env.IP_HASH_SECRET??'local-development-only';const hash=(v:string)=>createHash('sha256').update(`${secret}:${v}`).digest('hex');if(!await rateLimit([hash(ip),hash(email),hash(d.sessionId)]))return NextResponse.json({message:'Aguarde antes de enviar uma nova solicitação.'},{status:429});const publicId=`AUR-${randomBytes(5).toString('hex').toUpperCase()}`;const clean={...d,name:sanitizeText(d.name),email,phone,message:sanitizeText(d.message)};void sendContactEmails(clean,publicId);return NextResponse.json({message:'Recebemos sua mensagem.',protocol:publicId},{status:201})}catch(error){console.error(JSON.stringify({event:'contact_failed',kind:error instanceof Error?error.message:'unknown'}));return NextResponse.json({message:generic},{status:500})}}
