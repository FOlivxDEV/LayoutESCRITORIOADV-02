import{describe,expect,it}from"vitest";import{contactSchema,normalizeEmail,normalizePhone,sanitizeText}from"@/lib/validation/contact";import{auditAdvertising,hasExcessiveUrls}from"@/lib/security/compliance";import{can}from"@/lib/auth/permissions";import{whatsappUrl}from"@/config/site";
const valid={name:'Ana Silva',email:'ANA@EXAMPLE.COM',phone:'(11) 99999-9999',subject:'Direito Trabalhista',message:'Preciso de informações gerais.',privacyAccepted:true,policyVersion:'2026-07',website:'',startedAt:Date.now()-5000,sessionId:'1234567890123456'} as const;
describe('contact validation',()=>{it('accepts valid payload',()=>expect(contactSchema.safeParse(valid).success).toBe(true));it('rejects messages over 2000 chars',()=>expect(contactSchema.safeParse({...valid,message:'a'.repeat(2001)}).success).toBe(false));it('rejects invalid subject and missing consent',()=>expect(contactSchema.safeParse({...valid,subject:'Outro',privacyAccepted:false}).success).toBe(false));it('normalizes contact data',()=>{expect(normalizeEmail(' ANA@EXAMPLE.COM ')).toBe('ana@example.com');expect(normalizePhone('(11) 99999-9999')).toBe('11999999999')});it('sanitizes angle brackets',()=>expect(sanitizeText('<script>olá</script>')).not.toContain('<'));it('audits prohibited advertising',()=>expect(auditAdvertising('Resultado garantido')).toHaveLength(1));it('detects excessive urls',()=>expect(hasExcessiveUrls('http://a http://b http://c http://d')).toBe(true));it('generates encoded WhatsApp URL',()=>expect(whatsappUrl()).toMatch(/^https:\/\/wa\.me\/\d+\?text=/));it('enforces permissions',()=>{expect(can('editor','edit_content')).toBe(true);expect(can('editor','manage_users')).toBe(false)})})
